pipeline {
    agent any
    stages{
        stage('Test') {
            steps{
                sh 'whoami'
                sh 'pwd'
                sh 'ls -la'
                sh 'ls -ltr'
                sh 'ifconfig'
                sh 'uname -a'
            }
        }
    }
}